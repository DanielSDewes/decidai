# Núcleo (obrigatório para o pipeline rodar)
numpy>=1.26
pandas>=2.0
scikit-learn>=1.3
pulp>=2.7

# Interface
streamlit>=1.30

# Explicabilidade (opcional — XAI degrada para feature_importances_ se ausente)
shap>=0.44

# LLM (opcional — usado só se ANTHROPIC_API_KEY estiver definida)
anthropic>=0.40

# Testes
pytest>=7.0
